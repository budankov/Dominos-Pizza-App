import React from "react";
import { StyleSheet, Text, View } from "react-native";

const DominosClubScreen = () => {
  return (
    <View>
      <Text>Dominos Club Screen</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default DominosClubScreen;
