import React from "react";
import { StyleSheet, Text, View } from "react-native";

const PizzaTrackerScreen = () => {
  return (
    <View>
      <Text>Pizza Tracker Screen</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default PizzaTrackerScreen;
