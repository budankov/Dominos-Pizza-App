import React from "react";
import { StyleSheet, Text, View } from "react-native";

const PizzeriasScreen = () => {
  return (
    <View>
      <Text>Pizzerias Screen</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default PizzeriasScreen;
