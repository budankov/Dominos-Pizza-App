import React from "react";
import { StyleSheet, Text, View } from "react-native";

const PizzaMakerScreen = () => {
  return (
    <View>
      <Text>Pizza Maker Screen</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default PizzaMakerScreen;
