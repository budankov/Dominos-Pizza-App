import React from "react";
import { StyleSheet, Text, View } from "react-native";

const ProfileGroupScreen = () => {
  return (
    <View>
      <Text>ProfileGroupScreen</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default ProfileGroupScreen;
