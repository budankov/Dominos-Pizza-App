import React from "react";
import { StyleSheet, Text, View } from "react-native";

const ProfileOrdersScreen = () => {
  return (
    <View>
      <Text>ProfileOrdersScreen</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default ProfileOrdersScreen;
